---
description: Build the production bundle
---

// turbo
1. Run `npm run build` to generate the production bundle in the `dist/` directory.
2. The build process uses Vite and Handlebars to compile all pages.
3. Assets will be minified and optimized.
