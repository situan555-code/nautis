---
description: Deploy to production (Vercel/Netlify)
---

1. Ensure the site builds correctly by running `npm run build`.
2. Connect the repository to your Vercel/Netlify account.
3. Set the build command to `npm run build` and the output directory to `dist/`.
4. Deploy the application to production.
