---
description: Start the Vite development server
---

// turbo
1. Run `npm run dev` to start the development server.
2. The site will be available at http://localhost:5173 by default.
3. Handlebars partials and HMR for JS/CSS are enabled.
